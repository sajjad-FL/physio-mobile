import { sp } from './spacing'

/** Layout constants aligned with web mobile breakpoint (client AppShell + index.css). */
export const layout = {
  screenPaddingX: sp[4],
  sectionGap: sp[4],
  cardPadding: sp[3],
  headerHeight: 64,
  tabIconSize: 20,
  tabLabelSize: 10,
  tabMinHeight: 48,
}
